# Flask Configuration
SECRET_KEY=^PzTID\TW|\9{D?g}W"*XYlc%dd&#>iC
FLASK_ENV=production
FLASK_DEBUG=False

# Database Configuration
DATABASE_URL=sqlite:///instance/duet_events.db
# For PostgreSQL on Render: postgresql://username:password@hostname:port/database
# Render will provide this URL when you create a PostgreSQL database

# Server Configuration (Render handles this automatically)
PORT=5000
HOST=0.0.0.0

# Email Configuration (Optional - for email features)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Security Configuration (Important for Production)
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax

# Upload Configuration
MAX_CONTENT_LENGTH=16777216  # 16MB in bytes
UPLOAD_FOLDER=instance/uploads

# Admin Configuration (CHANGE THESE!)
ADMIN_EMAIL=admin@duet.ac.bd
ADMIN_PASSWORD=admin123

# Render-specific settings
PYTHON_VERSION=3.11.0
